---Para obtener los permisos de un usuario en una entidad (por ejemplo, una sucursal o centro de costo), podemos ejecutar el siguiente código

EXEC GetUserPermissions @entitycatalog_id = 1, @user_id = 123;